'use strict';

// const assert = require('assert');
let expect = require('chai').expect;

describe('Service test suite', function() {
  	describe('#test', function () {
  		it('should always success', function () {
  			expect(true).to.be.true;
  		});
  	});
});